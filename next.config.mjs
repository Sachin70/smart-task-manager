/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // Setting ignoreBuildErrors to true will allow the build to continue even if there are TypeScript errors.
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
